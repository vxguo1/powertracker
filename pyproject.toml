[project]
name = "powertracker"
version = "0.1.0"
description = "Track US electricity grid demand around AI data centers"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
