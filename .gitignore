# Secrets
.env
.env.local

# Data (raw pulls + rendered artifacts — regenerable from the scripts)
data/raw/
data/maps/
data/tiles_tmp/
data/cache/noaa_storm_events/
# Power infrastructure geojson caches — regenerated by
# scripts/fetch_power_infra.py before each tile build. transmission_lines
# is ~130 MB (over GitHub's 100 MB hard limit); substations is ~14 MB.
data/cache/substations.geojson
data/cache/transmission_lines.geojson
*.duckdb
*.duckdb.wal

# Claude (runtime state and per-user settings)
.claude/worktrees/
.claude/settings.local.json
.claude/launch.json

# Cloudflare / Wrangler
.wrangler/
.dev.vars

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
